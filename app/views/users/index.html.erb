<% provide(:title, "ユーザ一覧") %>
<% provide(:class_text, "user--edi") %>
<% provide(:button_text, "編集する") %>
<h1>ユーザ一覧</h1>
<div class="csv_holder">
  <%= form_tag import_users_path, multipart: true do %>
    <div><%= file_field_tag :file, class: "file-csv" %></div>
    <div><%= submit_tag "CSVをインポート", class: "btn btn-block btn-primary" %></div>
  <% end %>
</div>
<div>
  <p>Example</p>
</div>
<% if current_user.admin? %>
  <div class="example">
    <% @users.each do |user| %>
      <%= render partial: "shared/error_messages", collection: @user %>
      <div><%= user.name %></div>
      <div>
        <span>|</span>
          <%= link_to "削除", user, method: :delete,
              data: { confirm: "削除してよろしいですか？" },
              class: "btn btn-primary" %>
      </div>
      
      <div class="panel-group" id="accordion">
        <div class="panel panel-default">
          <div class="panel-heading">
            <h4 class="panel-title">
              <a data-toggle="collapse" data-parent="#accordion" href="#collapse<%= user.id %>">
              <button class="btn btn-primary btn-lg w-10" type="button">
                編集
              </button>
              </a>
            </h4>
          </div>
          <div id="accordion<%= user.id %>" class="panel-collapse collapse in">
            <div class="panel-body"><%= render partial: 'users/form_user', collection: @user, as: :user %></div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% end %>
